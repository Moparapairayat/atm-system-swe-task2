package atm;

/** <<control>> Maintenance (abstract) extends ServiceActivity */
public abstract class Maintenance extends ServiceActivity {
    public Maintenance(String activityID) {
        super(activityID);
    }
}
